#!/usr/bin/env python3
"""
Simple launcher for setup script.
Calls the actual setup.py in src/ directory.
"""

import sys
from pathlib import Path

# Add src to path
src_dir = Path(__file__).parent / "src"
sys.path.insert(0, str(src_dir))

# Run setup
import setup
setup.main()
